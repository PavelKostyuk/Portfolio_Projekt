apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-django-app-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-django-app
  template:
    metadata:
      labels:
        app: my-django-app
    spec:
      containers:
      - name: my-django-app
        image: pavelkostyuk/portfolio:latest
        ports:
        - containerPort: 8000
